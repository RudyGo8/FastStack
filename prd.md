需求如下：

1. 1.在sop分析的工作台展示数据需要需要四个条件 1>区域分国内和海外你确定下 2>SPU产品 3>周期月份筛选（默认展示过去6个月数据 4>销售渠道的筛选2.报表1为图表，报表2为表格，显示的都是上面四个条件筛选后的结果，其中报表1的选取数据为出库数量、激活数量、和提报预测数量（当月开始预测数量，如果月份已经过去就不要展示）
2. 数据中心的数据库已经更新了，可看项目文件夹内的 /docs 里面的sop数据清单里面的东西3.目前的团队方案是AI预测由其他部门进行数据接入，后面提供一个数据表就行，所以现在AI预测不需要了，在预测校验上面可以提示后期介入3.代码要简洁精简，严禁杜绝冗余代码。

---

## 缺失数据说明（2026-09-15 排查）

### 1. 激活数据（0 条，状态：待适配）

- **数据源**：`big_data_dw.dw_c_base_product_header`、`dw_c_base_product_events`、`dw_a_device`
- **连接状态**：✅ big_data_dw 连接正常，所有表可访问（918 万 / 764 万 / 699 万条记录）
- **ETL SQL**：`warehouse_queries.py` 中 `ACTIVATION_ETL_SQL` 已存在（良发提供的数仓脚本）
- **未同步原因**：`sync_from_warehouse()` 方法只同步了 SPU/预测/销售，未调用激活数据同步
- **待解决**：
  - `ACTIVATION_ETL_SQL` 返回 SN 级明细数据（含 25+ 字段），需聚合为 `sop_activation_daily` 表结构（spu_code + business_date + region + channel + activation_qty）
  - Excel 备注：过滤位置、去重主键和 SPU 编码需确认后固化为只读视图
  - 需要业务数据团队（良发）确认映射关系后再实施

### 2. 销售数据渠道缺失（只有"全球/全部渠道"）

- **现状**：`sop_sales_daily` 只有 10,324 条"全球/全部渠道"聚合数据
- **预测数据**：`sop_forecast_monthly` 有 7 个渠道明细（国内电商、国际渠道销售一部/二部、海外电商、跨境、国内渠道、国际品牌营销部）
- **问题**：`sync_from_warehouse()` 中销售数据硬编码 `region="全球", channel="全部渠道"`，未从数仓提取渠道字段
- **待解决**：需要从 `dw_a_sal_order_*` / `dw_a_sal_out*` 正确提取渠道字段，按渠道拆分导入

### 3. 数仓同步功能缺失

| 数据域         | 状态        | 说明                 |
| -------------- | ----------- | -------------------- |
| SPU 主数据     | ✅ 已同步   | 31 条                |
| 历史销售与出库 | ⚠️ 部分同步 | 有数据但缺渠道维度   |
| 预测数据       | ✅ 已同步   | 1,185 条，7 个渠道   |
| 激活数据       | ❌ 未同步   | ETL SQL 存在但未调用 |
| 库存与在途     | ❌ 未同步   | SQL 存在但未调用     |
| 竞品数据       | 未同步      | 需人工填报或系统接入 |
| 产能约束       | ❌ 未同步   | 二期预留             |
