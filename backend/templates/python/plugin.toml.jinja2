# 插件元数据（可选）：供文档、运维与 module_application.portal 展示使用。
# 不用于运行时 pip 安装依赖；依赖见仓库根目录 pyproject.toml / uv.lock。

name = "{{ plugin_name }}"
title = "{{ function_name }}"
version = "1.0.0"
description = "{{ function_name }}模块（{{ module_name }}），由代码生成器产出。"
optional = true
tags = ["generated"]
